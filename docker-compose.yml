version: '3.8'

services:
  # 1. MongoDB Database
  mongodb:
    image: mongo:7.0
    container_name: gridpilot-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - gridpilot-network

  # 2. Python FastAPI Optimization Engine (Member C)
  engine:
    build:
      context: ./engine
      dockerfile: Dockerfile
    container_name: gridpilot-engine
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
    networks:
      - gridpilot-network

  # 3. Node.js + Express API Gateway (Member B)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: gridpilot-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - HOST=0.0.0.0
      - NODE_ENV=production
      - MONGODB_URI=mongodb://mongodb:27017/gridpilot
      - JWT_SECRET=super_secret_gridpilot_production_key_2026
      - ENGINE_BASE_URL=http://engine:8000
      - USE_MOCK_ENGINE=false
      - ORCHESTRATOR_TICK_MS=5000
      - FRONTEND_ORIGIN=*
    depends_on:
      - mongodb
      - engine
    networks:
      - gridpilot-network

networks:
  gridpilot-network:
    driver: bridge

volumes:
  mongo_data:

